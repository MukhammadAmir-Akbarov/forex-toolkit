# TODO: рабочий чек-лист

> Обновлено: **2026-08-09**. Это единственный исполнимый список проекта.
> Что уже сделано — в [HISTORY.md](HISTORY.md) и [SESSION-2026-08.md](SESSION-2026-08.md).
> Разбор прошлых волн с оценками — в [BACKLOG.md](BACKLOG.md).
> Технические ловушки — в [ENGINEERING-NOTES.md](ENGINEERING-NOTES.md).

**Легенда.** `S` — до 2 часов · `M` — 2–5 часов · `L` — 5–10 часов.
Все оценки — рабочего времени, без CI и деплоя (+15 минут на PR).

## Правило, которое не отменяется

Крупные функции не добавляются, пока не собрана неделя метрик
([IMPROVEMENTS.md](IMPROVEMENTS.md)). Исключения — защита данных, доступность
и подтверждённые ошибки расчёта. У каждого пункта ниже помечено, попадает он в
исключение (**можно сразу**) или ждёт данных (*ждёт метрик*).

---

## Требуются действия владельца

- [ ] **G1. PyPI.** Тег `v0.1.0` создан 22 мая 2026, пакет отдаёт 404 — релиз
  так и не выпущен. Порядок — в [RELEASE-CHECKLIST.md](RELEASE-CHECKLIST.md).
- [x] **G2. giscus. Готово 2026-08-09.** Приложение установлено, блок включён,
  комментарии работают на трёх локалях. Грузятся лениво — по долистыванию до
  блока, а не на каждой из 365 страниц. Детали — [GISCUS-SETUP.md](GISCUS-SETUP.md).
- [ ] **G3. Неделя метрик GoatCounter.** [tools/funnel_report.py](tools/funnel_report.py)
  написан ровно для этого и простаивает. **Это следующий шаг по важности
  вообще:** он говорит, какой пункт делать первым, вместо догадки.
- [ ] **N4. Запуск.** Семь готовых постов в [LAUNCH.md](LAUNCH.md) не
  опубликованы ни разу.

---

## Открытое из прошлых волн

| # | Что | Состояние |
|---|---|---|
| C2 | Высота страницы журнала на телефоне — 13 404px | продуктовое решение: что показывать первым, что сворачивать · `M` · *ждёт метрик* |
| C4 | Сравнение с реалистичным ориентиром winrate | нужен надёжный источник, иначе выдуманный «средний трейдер» · `M` |
| D5 | Пере-проверка региональных фактов по УЗ | **обязанность, срок до декабря 2026** · `S`, раз в полгода |
| E3 | Поглавные мини-квизы | механика вопросов уже общая после E2 · `M` · *ждёт метрик* |
| E4 | Сертификат с датой, процентом и версией банка | ограниченная ценность без сервера · `S` · P3 |

---

## H. Ещё заперто за установкой Python

Самая продуктивная жила прошлых циклов: так на сайт попали тест готовности,
проверка брокера, переобучение и рейтинг стратегий.

- [ ] **H1. Месячный отчёт журнала.** [journal/monthly_report.py](journal/monthly_report.py)
  — 222 строки. В вебе есть недельный отчёт и «три дорогие привычки», но
  помесячной агрегации нет (в `journal.js` ноль `byMonth`). Отсюда же R-кривая
  по месяцам: «стал ли я лучше за полгода». Единственный пункт волны 2, который
  так и не закрыт. · `M` · ~4 ч · *ждёт метрик*
- [ ] **H2. Срезы из [tools/journal_analyzer.py](tools/journal_analyzer.py).**
  Час дня, день недели, сетап, влияние «следовал правилам». Часть срезов в вебе
  уже есть — надо сверить и дотянуть недостающие, а не портировать вслепую.
  · `S` · ~2 ч · *ждёт метрик*
- [ ] **H3. Банк эпизодов Replay использует половину данных.** 80 эпизодов на
  четырёх парах (EURUSD, GBPUSD, USDJPY, EURJPY), а в `data/` лежат ещё AUDUSD,
  GBPJPY, NZDUSD, USDCAD. [tools/replay_cutter.py](tools/replay_cutter.py) уже
  умеет резать — это прогон и обновление JSON. · `S` · ~1.5 ч · **можно сразу**
- [ ] **H4. Экспорт колоды Anki.** README обещает «Anki-карточки», в
  [flashcards.md](_mkdocs/tools/flashcards.md) экспорта нет вообще. Кнопка
  «скачать .csv для Anki» закрывает обещание. · `S` · ~1 ч

## I. Тренажёры и калькуляторы, которых нет

- [ ] **I1. Тренажёр «поставь стоп».** Незакрытая точка маршрута: Replay
  тренирует вход, position-management — ведение, а *размер* стопа нигде.
  Механика: график → студент ставит SL/TP → сравнение с ATR-нормой и с тем, что
  было дальше в архиве. Данные и рендер свечей уже есть. · `M` · ~5 ч ·
  *ждёт метрик*
- [ ] **I2. Сколько стоит подписка на сигналы или prop-челлендж.** Сколько R в
  месяц надо отбить, чтобы выйти в ноль по подписке $50–200. Защита аудитории
  цифрой, а не уговорами. · `S` · ~2 ч
- [ ] **I3. Устный тренажёр риска.** Десять задач «депозит X, риск 1%, стоп 30
  пунктов — лот?» с таймером. Формулы в [fx_math.py](forex_toolkit/fx_math.py)
  уже зеркалятся в JS. · `S` · ~2 ч · *ждёт метрик*

## J. Контентные пробелы — защита аудитории

- [ ] **J1. Prop-фирмы (FTMO-подобные челленджи).** Ни одного упоминания в
  проекте. Сейчас это главный канал денег молодой аудитории и главная зона
  скама. Математика челленджа считается тем, что уже есть:
  [monte_carlo.py](forex_toolkit/monte_carlo.py) и
  [overfitting.py](forex_toolkit/overfitting.py). · `M` · ~4 ч
- [ ] **J2. Копитрейдинг, PAMM, «наставники».**
  [scam-protection.md](_mkdocs/uz/scam-protection.md) покрывает откровенный
  развод, но не серую зону, куда идут после первого слива. · `M` · ~3 ч
- [ ] **J3. «Я слил депозит — что делать».**
  [anti-tilt-protocol.md](_mkdocs/extras/anti-tilt-protocol.md) — про эмоцию до
  сделки, [responsible-trading.md](_mkdocs/extras/responsible-trading.md) — про
  зависимость. Про сам момент после потери, когда человек ищет «отыграться»,
  страницы нет. · `S` · ~2 ч

## K. Витринные данные — честность и воспроизводимость

- [ ] **K1. Нет скрипта пересборки `_mkdocs/data/*.json`.** Четыре витрины
  (overfitting, multipair, strategy-ranking, replay-episodes) построены на
  числах, которые никто не может пересчитать одной командой. Нужен
  `tools/rebuild_showcase_data.py` и запись в JSON: пара, таймфрейм, период
  выборки, дата расчёта — традиция из [ENGINEERING-NOTES §1.3](ENGINEERING-NOTES.md).
  · `M` · ~3 ч · **можно сразу** (честность вывода)
- [ ] **K2. Гейт свежести витринных JSON.**
  [tools/check_freshness.py](tools/check_freshness.py) следит только за
  `verified` в markdown; данные за два года устареют молча. · `S` · ~1.5 ч ·
  **можно сразу**
- [ ] **K3. Период до 5–10 лет и кризисные окна** (март 2020, 2022). Вывод
  «настройка не переносится» станет заметно убедительнее, котировки качает
  [data_downloader.py](advanced/data_downloader.py). · `M` · ~3 ч

## L. Тесты: где ещё слепые зоны

518 unit и 213 браузерных сценариев (на 2026-08-09) — но:

- [x] **L1. Мутационное тестирование финансового ядра.** Первый прогон:
  159 мутантов, **26 выживших** — каждая шестая подмена не роняла ни один
  тест. Среди них: риск ровно по лимиту, безубыток как убыток, микро-лот как
  недопустимый ввод, округление лота вверх. Все закрыты, сейчас 159 из 159
  убито, гейт в CI держит результат. Разбор — [tools/mutation.md](tools/mutation.md).
- [ ] **L2. Property-based тесты (hypothesis).** Инварианты: риск никогда не
  больше заданного, лот ≥ 0, монотонность лота по стопу, pip-value для JPY и
  не-JPY. Ошибка с размером пункта иены (см. SESSION-2026-08) ловилась бы
  автоматически. · `M` · ~4 ч · **можно сразу**
- [x] **L3. Гейт размера JS.** B1 срезал глобальный JS, но ничто не мешало
  вырасти обратно. Бюджет — 20 KB gzip на всё, что грузится на каждой
  странице (сейчас 17.9). Заодно нашлись сироты: `mobile-menu-hint.js` не
  подключался ниоткуда, хотя его CSS ездил на всех страницах.
- [ ] **L4. Скриншот-регрессии на 4–5 экранов.** Три подряд бага были чисто
  визуальные (z-index переключателя языков дважды, обрезка журнала) — их не
  ловит ни один текущий тест. · `M` · ~4 ч · **можно сразу** (доступность)
- [ ] **L5. axe-core по состояниям виджетов.** Lighthouse меряет страницу
  целиком и пропускает открытое модальное и ошибку формы. · `S` · ~2 ч ·
  **можно сразу**
- [ ] **L6. Lighthouse на медленной сети** (старый F2): профиль 3G + 390px.
  · `M` · ~3 ч · **можно сразу**

## M. Инфраструктура и безопасность

- [x] **M1. Внешние ссылки никто не проверял.**
  [tools/check_links.py](tools/check_links.py) сознательно пропускает `http(s)`,
  а проект ведёт на реестры FCA/CySEC/ASIC/NFA/FINMA и 12 брокеров. Мёртвая
  ссылка на регулятор бьёт по доверию сильнее битого якоря. Добавлен
  [tools/check_external_links.py](tools/check_external_links.py) и
  неблокирующий еженедельный джоб.
- [ ] **M2. Свежесть за пределами `uz/`.** `SENSITIVE_PREFIXES` в
  check_freshness покрывает только `uz/`, а
  [brokers-comparison.md](_mkdocs/extras/brokers-comparison.md) и
  [market-data-sources.md](_mkdocs/extras/market-data-sources.md) стареют так
  же быстро и `verified`-метки не имеют. Ставить метку можно **только после
  фактической проверки содержимого**, иначе это ложное обещание свежести.
  · `S` · ~1 ч + проверка фактов · **можно сразу**
- [x] **M3. pip-audit и CodeQL.** Dependabot обновлял версии, но про CVE в уже
  стоящих молчал, а SAST не было вовсе — при том что проект принимает
  недоверенный ввод в двух местах: чужие MT5-отчёты и чужой backup, где уже
  была XSS (PR #52). CodeQL смотрит поток данных по Python и по браузерным
  виджетам, pip-audit — блокирующий шаг еженедельной проверки.
- [ ] **M4. Подпись релиза (sigstore) и SBOM** при публикации на PyPI.
  · `S` · ~2 ч
- [ ] **M5. Гейт покрытия по модулю.** Общие 92% скрывают
  `risk_exposure.py` — 86%. · `S` · ~1.5 ч · **можно сразу**

## N. Дистрибуция и рост

- [x] **N1. PDF собирался только на русском.** `release.yml` вызывал
  `build_pdf.py` без `--lang` и клал в релиз один `forex-handbook.pdf`, хотя
  скрипт умеет `ru/en/uz`. Для основной аудитории узбекский PDF был написан,
  собран ни разу. Теперь релиз собирает и публикует все три.
- [ ] **N2. Telegram Mini App: калькулятор позиции и «Перед сделкой».**
  Аудитория живёт в Telegram, инфраструктура почти вся есть
  ([telegram_alerts.py](advanced/telegram_alerts.py),
  [coach_bot.py](advanced/coach_bot.py)), виджеты уже offline-first и без
  бэкенда. Самый крупный рычаг роста и самый дорогой пункт списка.
  · `L` · ~10 ч · *ждёт метрик*
- [ ] **N3. EPUB рядом с PDF/DOCX** — читается на телефоне без зума, в отличие
  от A4-PDF. · `S` · ~2 ч

## O. Маршрут и удержание

- [ ] **O1. «Что дальше» после экзамена.** Маршрут кончается PNG-сертификатом.
  Нужен 30-дневный план с чек-инами в кабинете — содержание уже написано в
  [first-100-days.md](_mkdocs/extras/first-100-days.md), не хватает механики.
  · `M` · ~4 ч · *ждёт метрик*
- [ ] **O2. Воскресный ритуал.** Недельный отчёт есть, триггера нет:
  локальное напоминание в кабинете, как сделано для декларации. · `S` · ~2 ч
- [ ] **O3. Выписка журнала в PDF** для бухгалтера поверх готового годового
  итога из D1. · `S` · ~2 ч

## P. Гигиена

- [x] **P1. Отчётные файлы снова расползались.** TODO, BACKLOG, IMPROVEMENTS,
  SESSION и HISTORY пересказывали друг друга — ровно то, что G4 уже сводил
  однажды. TODO.md стал единственным исполнимым списком, остальные — история и
  разбор.
- [x] **P2. Числа в README разошлись с кодом.** Обещалось «156 unit-тестов»
  при 502 и «25+ Python-инструментов» при 32. Хук
  [project_stats.py](hooks/project_stats.py) считает это для сайта, но README
  не трогает.
- [ ] **P3. Синхронизация `.codex/skills/graphify/`** с `.claude/` — CLAUDE.md
  фиксирует, что они уже разошлись. · `S` · ~1 ч
- [ ] **P4. Удалить 16 смерженных веток.** · `S` · 15 мин

---

## Что делать первым

1. **G3 — неделя метрик.** Пунктов выше больше сорока, и ни одного
   доказательства, какой из них ждут. `funnel_report.py` простаивает.
2. **M2** — после фактической проверки содержимого двух страниц.
3. **L1** — единственная непроверенная проверка в самом дорогом месте проекта.
4. **K1 + K2** — витрины должны пересчитываться одной командой и стареть громко.
5. Из крупного, когда метрики придут: **J1** (prop-фирмы) как контент и
   **I1** (тренажёр стопа) как последняя дыра маршрута.

## Проверки

- [x] Unit: **518 тестов**.
- [x] Playwright: **213 сценариев**.
- [x] ruff/format для `tests/` и `tests_e2e/` в CI.
- [x] Translation coverage/drift: 89/89/89 исходных страниц.
- [x] Freshness, внутренние ссылки и `mkdocs build --strict` проходят.
- [x] Внешние ссылки — еженедельно, неблокирующе.
