# Идеи и план работ — полный разбор проекта

> Составлено **2026-08-07** после сплошного прохода: 83 RU-страницы × 3 языка
> (249 md), 21 браузерный виджет (6288 строк JS), 13 модулей пакета,
> 29 инструментов в `tools/`, advanced-слой, 253 unit + 83 браузерных теста,
> 12 гейтов CI.
>
> Каждый пункт проверен в коде, а не предположен. Где я что-то измерил —
> цифра стоит рядом. Где идея требует данных или решения владельца — сказано
> прямо.

**Легенда.** `S` — до 2 часов · `M` — 2–5 часов · `L` — 5–10 часов.
Часы — моего рабочего времени; к каждому PR добавляется ~15 минут CI и деплой.

---

## Статус на 2026-08-08

**Волна 1 закрыта полностью**, волна 2 — 6 пунктов из 7 (остался A3).

| PR | Пункты | Что сделано |
|---|---|---|
| [#58](https://github.com/MukhammadAmir-Akbarov/forex-toolkit/pull/58) | B2, B1, F1, F3 | Подтверждение перед очисткой журнала; JS на текстовой странице 38 → 2.4 KB; видимый фокус; три страницы в навигацию |
| [#59](https://github.com/MukhammadAmir-Akbarov/forex-toolkit/pull/59) | B3, B5 | eslint на 6300 строк виджетов; `mt5_statement` 89 → 96% |
| [#60](https://github.com/MukhammadAmir-Akbarov/forex-toolkit/pull/60) | D1 | Годовой итог из журнала в калькулятор налога |
| [#61](https://github.com/MukhammadAmir-Akbarov/forex-toolkit/pull/61) | D2 | Чек-лист исламского счёта в проверке брокера |
| [#62](https://github.com/MukhammadAmir-Akbarov/forex-toolkit/pull/62) | A1 | Три самые дорогие привычки за месяц |
| [#63](https://github.com/MukhammadAmir-Akbarov/forex-toolkit/pull/63) | D4 | Сколько капитала нужно, чтобы жить с трейдинга |
| [#64](https://github.com/MukhammadAmir-Akbarov/forex-toolkit/pull/64) | D3, E1, C3, G4, E2 | Четыре мелких пункта, инженерные заметки, банк экзамена 30 → 45 |
| [#65](https://github.com/MukhammadAmir-Akbarov/forex-toolkit/pull/65) | B4 | Сверки виджетов в Node вместо браузера — 33 проверки за 1.4 с |
| [#66](https://github.com/MukhammadAmir-Akbarov/forex-toolkit/pull/66) | — | Переключатель языков перестал свисать на соседние вкладки |

Тесты: **253 → 424 unit**, **83 → 160 браузерных**. Четыре новых модуля пакета
(`tax_summary`, `habits`, `living_capital`, `pattern_outcomes`) — каждый с
зеркалом в браузере и сверкой, по традиции проекта.

**Осталось:** A3 из волны 2 и вся волна 3 — C1 (открытая позиция в Replay, ~8 ч),
A4 (демонстрация переобучения, ~6 ч), A3 (сравнение стратегий на реальном
архиве, ~6 ч), C2 (мобильный ввод сделки, ~4 ч), A5 (мульти-пары, ~3 ч).
Итого около 27 часов.

**Что я советую перед тем, как брать остаток:** прогнать неделю метрик
(`tools/funnel_report.py`). Пункты ниже осмысленные, но какие из них люди ждут —
до сих пор неизвестно.

---

## Правило проекта, которое я не отменяю

`IMPROVEMENTS.md` говорит: **крупные функции не добавляются, пока не собрана
неделя метрик**. Исключения — защита данных, доступность и подтверждённые
ошибки расчёта.

Поэтому список разделён: часть пунктов попадает в исключения и делается сразу,
остальные ждут данных. Это отмечено у каждого.

---

## Что уже проверено и отклонено

Чтобы следующий цикл на это не тратился.

| Идея | Почему нет |
|---|---|
| Портировать тренер `coach.py` | Журнал уже даёт те же семь выводов |
| Калькулятор вывода денег | Уже встроен в гид по выводу во всех трёх локалях |
| Упражнения «управление позицией» в очереди | Ложная тревога: задача называется «стоп до входа», Replay это умеет |
| Портировать `forex-news` | Требует живого источника, проект сознательно отложил |
| Портировать `forex-correlations` | Покрыт калькулятором совокупного риска |
| Свои графики котировок | Ноль ценности сверх TradingView, зато вечное обслуживание |
| Свой генератор эпизодов Replay | **Уже есть** `tools/replay_cutter.py` — я это выяснил, написав дубликат и удалив его |

---

## A. Написано, но заперто за установкой Python

Самая продуктивная жила прошлого цикла: так на сайт попали тест готовности и
проверка брокера. В репозитории лежит ещё около 1300 строк рабочей логики,
которую аудитория с телефона никогда не увидит.

**A1. Месячный отчёт журнала.** `journal/monthly_report.py` — 222 строки:
агрегация по месяцу плюс графики. Веб-журнал умеет только недельный отчёт.
Этим же закрывается «три самые дорогие привычки за месяц» из старого списка:
журнал уже хранит стратегию, эмоцию, соблюдение правил и перенос стопа — не
хватает только агрегации. · `M` · **P0** · ~3–4 ч · *ждёт метрик*

**A2. Тренажёр свечных паттернов.** `tools/pattern_scanner.py` — 334 строки,
находит десять паттернов: молот, падающая звезда, поглощение, доджи, пин-бар,
утренняя и вечерняя звезда, три солдата и три вороны. Детекция готова, правила
простые и чистые (легко зеркалить в JS), Replay уже умеет рисовать свечи и
принимать ответ, а 80 эпизодов реальных котировок уже лежат в репозитории.

⚠️ **Важно, каким он должен быть.** Собственная страница проекта говорит:
«**Никогда не торговать только по паттерну**». Тренажёр вида «назови фигуру»
учил бы ровно обратному тому, откуда он растёт, и противоречил бы экзамену,
где сказано, что стабильно угадывать направление не получается ни у кого.

Честная версия: студент называет паттерн, а тренажёр **сразу показывает, чем
дело кончилось в архиве** — доля случаев, когда после этой фигуры цена пошла в
«обещанную» сторону. Тогда упражнение учит не вере в фигуры, а тому, что
распознавание и предсказание — разные вещи. Механика та же, что у проверки
«навык или везение». · `M` · P1 · ~5 ч · *ждёт метрик*

**A3. Сравнение стратегий.** `strategies/compare.py` (218 строк) плюс пять
стратегий (`breakout`, `breakout_v2`, `london_open`, `mean_reversion`,
`three_soldiers`). На сайте только текстовые описания.

⚠️ **Оценка выросла после проверки кода.** `compare.py` считает на
**синтетических** свечах (`generate_synthetic`), а не на архиве. Портировать
его как есть значит показать выдуманные числа как результаты стратегий — та же
нечестность, что устаревшие данные о брокерах. Делать надо на реальных CSV, как
`replay_cutter.py`, с явным указанием пары, таймфрейма и периода выборки. Это
сближает задачу с A4. · `L` · P2 · ~6 ч · *ждёт метрик*

**A4. Демонстрация переобучения.** `advanced/walk_forward.py` и
`parameter_sweep.py` показывают, что красивый бэктест обычно означает подгонку.
Именно на этом продают «роботов» и «сигналы» — то есть это прямая защита
аудитории от развода. Ограничение честное: `data/*.csv` в `.gitignore`, в CI не
посчитать, поэтому результат придётся зафиксировать как данные и один раз
проверить руками. · `L` · P1 · ~6 ч · *ждёт метрик*

**A5. Мульти-парный бэктест.** `advanced/multi_pair_backtest.py` (259 строк):
стратегия, работающая на EURUSD, разваливается на других парах. Хорошее
продолжение A4. · `M` · P2 · ~3 ч · *ждёт метрик*

---

## B. Измеренные дефекты

Здесь не «было бы неплохо», а числа.

**B1. 34 KB лишнего JavaScript на каждой странице.** Замерено: глобально
подключены 14 файлов = **38 KB gzip**, из которых везде нужны только
`_i18n.js` и `_training-queue.js` (2.4 KB). Остальные двенадцать —
одностраничные:

| файл | gzip | нужен на |
|---|---|---|
| `monte-carlo.js` | 6.3 KB | одной странице |
| `exam.js` | 5.1 KB | одной странице |
| `position.js` | 3.8 KB | одной странице |
| `risk-exposure.js` | 3.1 KB | одной странице |
| ещё восемь | ~16 KB | по одной странице |

Прошлый заход перевёл на постраничную загрузку 5 виджетов и остановился —
механизм (`widgets:` во front matter) уже работает, осталось дописать его для
остальных. Для аудитории на мобильном интернете это заметно.
· `S` · **P0** · ~1.5 ч · **можно сразу**

**B2. Кнопка «Очистить журнал» стирает всё без подтверждения.**
`journal.js:1397` вызывает `localStorage.removeItem` сразу по клику. Данные
живут только в браузере, отменить нельзя. При этом сброс в кабинете
подтверждение спрашивает (`dashboard.js:578`) — то есть это не решение, а
пропуск. Попадает в исключение «защита данных».
· `S` · **P0** · ~1 ч · **можно сразу**

**B3. Нет линтера для JavaScript.** 6288 строк в 21 виджете; CI линтует только
Python. Уязвимость с `innerHTML` из PR #52 — ровно тот класс ошибок, который
eslint ловит правилом. · `S` · P1 · ~2 ч · **можно сразу** (качество кода)

**B4. Виджеты покрыты только сквозными тестами.** Вся логика — риск-бюджет,
вердикты, агрегации — проверяется через браузер: 83 теста идут 2.5 минуты и
ходят в основном по счастливому пути. Чистые функции можно вынести и покрыть
быстрыми тестами. · `M` · P2 · ~4 ч

**B5. Слабые места покрытия пакета.** Всего 92%, но `risk_exposure.py` — 86%,
`mt5_statement.py` — 89%. Второй разбирает чужие отчёты, то есть работает с
недоверенным вводом: непокрытые ветки там дороже всего.
· `S` · P2 · ~2 ч · **можно сразу** (качество)

---

## C. Разрывы маршрута

**C1. Что делать во время сделки.** Путь закрыт с обеих сторон: «Перед сделкой»
— план и риск, журнал — разбор. Середина только текстом. Replay тренирует вход,
дальше исход считается сам. При этом перенос стопа журнал засчитывает как
нарушение и выдаёт за него задачу — а тренировать причину нечем. Нужен режим
«веди открытую позицию»: безубыток, частичное закрытие, трейл.
· `L` · P1 · ~8 ч · *ждёт метрик*

**C2. Мобильный ввод сделки.** Основной сценарий — записать сделку с телефона
сразу после закрытия. Форма разбора рассчитана на десктоп.
· `M` · P2 · ~4 ч · *ждёт метрик*

**C3. Журнал не учитывает стоимость вывода.** Показывает прибыль, которой нет
на карте: комиссия за вывод и конвертация съедают результат новичка.
Калькулятор вывода существует — не хватает связи. · `M` · P2 · ~3 ч

**C4. Сравнение с реалистичным ориентиром.** Новичок не знает, что 55% winrate
— это нормально, и бросает после обычной серии убытков. Требует надёжного
источника цифр, иначе получится выдуманный «средний трейдер».
· `M` · P2 · ~3 ч + поиск источника

---

## D. Узбекистан — раздел, который недоиспользован

Это единственное, чего нет у конкурентов, и именно здесь связи слабее всего.

**D1. Журнал не связан с налогом.** Проверено: в `journal.js` ноль упоминаний
налога. Журнал знает все сделки за год, налоговый калькулятор ждёт чистый
годовой результат, декларация подаётся **до 1 апреля** — и между ними нет
ничего. Кнопка «выгрузить годовой итог для декларации» закрывает реальную
боль. · `M` · **P0** · ~3 ч · **можно сразу** (региональный факт + данные)

**D2. Проверка брокера не знает про swap-free.** Проверено: в
`broker-check.js` ноль упоминаний свопа, при том что в проекте есть отдельная
страница `uz/swap-free.md`. Для аудитории, где это вопрос религиозный, а не
технический, фильтр «есть ли исламский счёт» — первый вопрос, а не последний.
· `S` · P1 · ~2 ч · **можно сразу**

**D3. Напоминание о сроке декларации.** До 1 апреля — а узнаёт человек об этом
из статьи, которую читал полгода назад. Локальное напоминание в кабинете
(январь–март) без сервера и аккаунта. · `S` · P2 · ~1.5 ч

**D4. Расчёт «сколько нужно капитала, чтобы жить с трейдинга».** Честный
холодный душ в местных цифрах: при реалистичных +1.5% в месяц и расходах в
сумах видно, что «жить с трейдинга» требует капитала, которого нет. Механика
сложного процента уже есть в `compound.js`, конвертация USD→UZS — в налоговом
калькуляторе. · `M` · P1 · ~3 ч

**D5. Пере-проверка региональных фактов.** Раздел про Узбекистан стареет
быстрее всего, CI начнёт падать при просрочке `verified`. **Срок — до декабря
2026.** Это обязанность, а не идея. · `S` · P1 · ~2 ч, повторять раз в полгода

---

## E. Экзамен и обучение (после этого захода)

**E1. Содержание квиза.** Структурную дыру я закрыл (перемешивание), но сами
вопросы остались уровня «что такое стоп-лосс» с отвлекающими вариантами вроде
«хвастаться прибылью». Он стоит перед экзаменом как быстрая самопроверка, и
делать его вторым экзаменом не нужно — но подтянуть до правдоподобных
вариантов стоит. · `S` · P2 · ~2 ч

**E2. Банк экзамена до 45 вопросов.** Сейчас 30, берётся 20. При 45 две попытки
подряд почти не пересекаются. Считать заново ничего не надо — только писать
вопросы на трёх языках. · `M` · P2 · ~3 ч

**E3. Поглавные мини-квизы.** Лежит в отложенном бэклоге. После A2 (паттерны) и
E2 механика вопросов будет общей — тогда это дёшево, сейчас нет.
· `M` · P3 · ~4 ч · *ждёт метрик*

**E4. Сертификат с проверяемой ссылкой.** Сейчас PNG рисуется локально и ничем
не подтверждается. Без сервера полноценной проверки не сделать, но можно
класть в файл дату, процент и версию банка. Ограниченная ценность — назвал для
полноты. · `S` · P3 · ~1.5 ч

---

## F. Доступность и качество вывода

**F1. Клавиатура в тесте готовности и проверке брокера.** В Replay доступность
доведена до конца; новые виджеты опираются на стандартные радио и чекбоксы —
рабочий минимум, но фокус и озвучивание результата стоит проверить.
· `S` · P1 · ~2 ч · **можно сразу** (доступность)

**F2. Проверка на реальном мобильном.** Lighthouse a11y в CI есть, замера на
узком экране с медленной сетью нет. Экзамен на 390px я проверял вручную —
это должно быть автоматикой, а не памятью. · `M` · P2 · ~3 ч

**F3. Три страницы вне навигации.** `extras/video-script-1-full.md`,
`video-script-4-full.md`, `video-scripts.md` — есть в сборке, но недостижимы
из меню. Либо убрать, либо связать. · `S` · P3 · ~0.5 ч · **можно сразу**

---

## G. Владелец и обслуживание

**G1. PyPI.** Пакет не опубликован — проверено, `pypi.org/pypi/forex-toolkit`
отдаёт 404. Тег `v0.1.0` при этом **создан** (22 мая 2026), то есть
`TODO.md` в этой части устарел: там сказано, что тег ждёт подтверждения.
· только владелец

**G2. giscus.** Установить GitHub App, затем раскомментировать готовый блок.
· только владелец

**G3. Неделя метрик GoatCounter.** `tools/funnel_report.py` готов и ждёт
реального экспорта. **Это следующий шаг по важности вообще**: он говорит,
какой из пунктов выше делать первым, вместо того чтобы гадать.
· только владелец (экспорт), дальше ~1 ч

**G4. Свести отчётные файлы. ✅ СДЕЛАНО.** `SESSION-REPORT.md`,
`WORK-REPORT.md`, `ADDED.md`, `AUDIT.md` и `IDEAS.md` пересказывали друг друга,
потому что писались по ходу. Сведены в [HISTORY.md](HISTORY.md); открытые
пункты перенесены сюда до удаления, входящих ссылок извне не было.

---

## План по волнам и сколько это займёт

### Волна 1 — можно делать прямо сейчас

Ничего не ждёт метрик: всё попадает в исключения (защита данных, доступность,
качество) или это региональный факт.

| # | Задача | Оценка |
|---|---|---|
| B2 | Подтверждение перед очисткой журнала | 1 ч |
| B1 | Убрать 34 KB лишнего JS со всех страниц | 1.5 ч |
| D1 | Годовой итог из журнала для декларации | 3 ч |
| D2 | Swap-free в проверке брокера | 2 ч |
| F1 | Клавиатура и озвучивание в двух новых виджетах | 2 ч |
| B3 | eslint для 6288 строк виджетов | 2 ч |
| F3 | Три страницы вне навигации | 0.5 ч |
| B5 | Дотянуть покрытие разбора чужих отчётов | 2 ч |

**Итого ~14 часов работы.** Это 3–4 PR (связанное лучше объединять), каждый с
прогоном 253 unit + 83 браузерных, CI и деплоем.

### Волна 2 — после недели метрик

Метрики скажут, какие из этих пунктов вообще нужны. Оценка — если делать все.

| # | Задача | Оценка |
|---|---|---|
| A1 | Месячный отчёт + три дорогие привычки | 4 ч |
| D4 | Сколько нужно капитала, чтобы жить с трейдинга | 3 ч |
| A3 | Сравнение стратегий | 3 ч |
| A2 | Тренажёр свечных паттернов | 4 ч |
| C3 | Стоимость вывода в статистике журнала | 3 ч |
| E1 | Подтянуть содержание квиза | 2 ч |
| D3 | Напоминание о декларации | 1.5 ч |

**Итого ~20 часов.**

### Волна 3 — крупное

| # | Задача | Оценка |
|---|---|---|
| C1 | Режим «веди открытую позицию» в Replay | 8 ч |
| A4 | Демонстрация переобучения | 6 ч |
| C2 | Мобильный ввод сделки | 4 ч |
| A5 | Мульти-парный бэктест | 3 ч |
| B4 | Быстрые тесты логики виджетов | 4 ч |
| E2 | Банк экзамена до 45 вопросов | 3 ч |

**Итого ~28 часов.**

### Сводка

| | работы | если по одной волне за заход |
|---|---|---|
| Волна 1 | ~14 ч | 2–3 сессии |
| Волна 2 | ~20 ч | 3–4 сессии |
| Волна 3 | ~28 ч | 4–6 сессий |
| **Всё** | **~62 ч** | **9–13 сессий** |

Календарём это зависит только от того, как часто ты запускаешь работу. Если по
одной сессии в день — примерно две недели на всё. Волна 1 закрывается за
2–3 дня.

**Честная оговорка про оценки.** Они по опыту этого проекта, но каждый раз
что-то находится по дороге: в этом заходе банк экзамена занял вдвое больше
ожидаемого, потому что вскрылась дыра с позицией ответа, а эпизоды Replay — в
полтора раза, потому что я сначала написал дубликат существующего генератора.
Закладывай +30% сверху.

---

## Что я советую делать первым

Не пункт из этого списка, а **G3 — прогнать неделю метрик**. Список выше
честный, но в нём двадцать восемь пунктов и ни одного доказательства, какой из
них люди действительно ждут. `funnel_report.py` написан именно для этого и
простаивает.

Если ждать не хочешь — бери **волну 1**. Она вся из исключений: её можно
делать не нарушая собственного правила проекта, и там два пункта (B2 и D1) я
считаю по-настоящему важными. B2 — потому что один клик стирает журнал
безвозвратно. D1 — потому что это единственное место, где проект решает задачу,
которую больше не решает никто.
