# IMPROVEMENTS: актуальный план развития

> Обновлено: **2026-08-06**. Исполнимые пункты находятся в [TODO.md](TODO.md).
> Легенда: **P0-P3** - приоритет; **S/M/L** - размер работы.

## Текущее состояние

- **81 RU + 81 EN + 81 UZ** исходная страница; 162 перевода отслеживаются drift-check.
- 171 unit-тест и 52 Playwright-сценария; всего 223 проверки выводятся на главной автоматически.
- Dashboard, прогресс, экзамен, Replay 2.0, веб-журнал 2.0 и Coach готовы.
- Позиция, пипс, маржа, стоимость, налог, совокупный риск и Monte Carlo работают в браузере.
- Экран «Перед сделкой» создаёт локальный план и напрямую открывает его в журнале.
- Журнал ведёт статусы План → Открыта → Закрыта, сохраняет разбор и подставляет
  личную статистику в Monte Carlo.
- Backup v3 защищает полный журнал, планы, лимиты риска, версии стратегий,
  очередь тренировок, настройки и учебные данные.
- «Первые 15 минут» даёт новому пользователю короткий маршрут на трёх языках.
- GoatCounter pageviews и анонимные события подключены; feedback работает на RU/EN/UZ.
- Основные страницы и калькуляторы доступны через service worker offline.
- Wheel/sdist и 8 CLI проверены локально; PyPI и giscus ждут владельца аккаунтов.

## Реализовано в этом цикле

### N. Автоматический риск-бюджет · P0 · ГОТОВО

Экран «Перед сделкой» считает риск открытых и запланированных позиций, итог после
новой сделки, дневной и недельный R, остаток лимитов и серию убытков. Настройки
живут локально. Превышение не блокирует пользователя, но требует отдельного
подтверждения и сохраняется вместе со снимком проверки.

### O. Контроль качества импорта · P0 · ГОТОВО

CSV/MT5 проходит preview до записи: число строк, ошибки чисел и дат, дубликаты,
отсутствующий риск, неизвестные колонки и первые 10 записей. Можно исключить
проблемные строки или вернуться к файлу. Персональный Monte Carlo использует
только сделки с достоверным R.

### P. Недельный отчёт · P1 · ГОТОВО

Журнал формирует USD/UZS/R за последнюю торговую неделю, сравнение с предыдущей,
дисциплину, лучшую и худшую стратегию, стоимость нарушений, слабую эмоцию и одну
задачу. Отчёт экспортируется в Markdown и имеет печатный режим.

### Q. Лаборатория стратегии · P1 · ГОТОВО

Правила хранятся как неизменяемые версии. План получает снимок конкретной версии,
а после целевой выборки лаборатория показывает expectancy, дисциплину, просадку
и общий R без смешивания разных вариантов стратегии.

### R. Персональная очередь Replay · P1 · ГОТОВО

Перенос стопа, FOMO и нарушение правил создают локальные задания по 10 упражнений.
Replay показывает активную задачу и сам увеличивает прогресс после подходящего
действия или рыночной категории.

### S. Доступность и локальное хранение · P0 · ГОТОВО

Replay поддерживает B/S/Space/стрелки, видимый фокус, текстовое описание графика
и таблицу свечей. Dashboard напоминает о backup через 30 дней, показывает статус
хранилища и запрашивает `navigator.storage.persist()`. Новая offline-версия
применяется только после нажатия пользователя, без самопроизвольной перезагрузки.

### T. Автоматические показатели · P2 · ГОТОВО

MkDocs hook считает исходные страницы, Python-инструменты, стратегии и развёрнутые
pytest cases во время сборки. Ручные числа на трёх главных страницах удалены.

### C. События и обратная связь · P0 · ГОТОВО

`_mkdocs/javascripts/analytics.js` принимает только заранее разрешённые события.
Параметры сделок, деньги, пары, имя экзамена и содержимое журнала не передаются.
События ставятся в очередь, если GoatCounter ещё загружается, и становятся no-op,
если счётчик заблокирован. Feedback использует штатную форму Material, хранит
ответ по pathname локально и не допускает повторного голоса.

Подробности приватности: `_mkdocs/privacy.md` + EN/UZ.

### F. Свежесть региональных данных · P0 · ГОТОВО

Tax calculator проверен по официальным сообщениям Налогового комитета от 2025
и 2026 годов. Tax и communities имеют `verified` на трёх языках. Команда
`tools/check_freshness.py --fail` теперь отклоняет и старые, и отсутствующие
метки во всём разделе `uz/`; CI использует строгий режим.

### D. Совокупный риск · P1 · ГОТОВО

Канон находится в `forex_toolkit/risk_exposure.py`:

- нормализация пары и long/short;
- ковариационная оценка статической корреляции;
- signed exposure для base и quote валют;
- равное/взвешенное распределение бюджета и расчёт лота;
- сводка лимита и остатка.

CLI сохраняет старые импорты, а веб-виджет показывает номинальный и эффективный
риск, USD/UZS, лоты, корреляции и валютную экспозицию. Статическая модель явно
обозначена как ориентировочная, не live-сигнал.

### G. Единый процесс перед сделкой · P1 · ГОТОВО

`tools/trade-desk` объединяет позиционный расчёт, обязательный checklist,
локальное сохранение планов, прямой переход в веб-журнал и запасной CSV-экспорт.
Журнал хранит первоначальную причину входа отдельно, переводит сделку по трём
статусам и после закрытия собирает результат, комиссию, эмоцию и дисциплину.

### H. Web Monte Carlo · P1 · ГОТОВО

`forex_toolkit/monte_carlo.py` и browser widget используют одинаковый Park-Miller
PRNG и percentile-алгоритм. Результат включает медиану, худшие 5%, просадки,
вероятность минуса, 20% drawdown, потерю половины капитала и серию убытков.
Одинаковый seed даёт воспроизводимый эксперимент. Из журнала автоматически
подставляются Win Rate и фактический R:R; рядом сравниваются риски 0,5%, 1% и 2%.

### K. Полный backup · P0 · ГОТОВО

Dashboard экспортирует backup v3 с полным журналом, планами, лимитами риска,
стратегиями, очередью тренировок, настройками, прогрессом, экзаменом и Replay.
Импорт принимает старые v1/v2, проверяет известную схему и типы данных и
показывает preview до записи в `localStorage`.

### L. Разбор после сделки · P1 · ГОТОВО

Форма закрытия сохраняет комиссию, эмоцию, соблюдение исходного плана, перенос
стопа и свободный вывод. По прозрачным локальным правилам формируется одна задача
на следующую неделю; ИИ, аккаунт и сервер не требуются.

### M. Первые 15 минут · P1 · ГОТОВО

Новая страница RU/EN/UZ связывает основу, позиционный калькулятор, Replay и первый
план. Шаги собираются из локальных событий, показывают следующий конкретный переход
и не создают ложного обещания «быстрой торговли».

### I. Offline PWA · P1 · ГОТОВО

Service worker кеширует app shell, базовый учебник, журнал и ключевые калькуляторы,
сохраняет посещённые страницы и показывает локализованный fallback без сети.
Manifest использует относительный scope, поэтому работает и на GitHub Pages, и локально.

### E. Локальный аудит PyPI · P1 · ГОТОВО, ПУБЛИКАЦИЯ ЗАБЛОКИРОВАНА ВЛАДЕЛЬЦЕМ

- `forex_toolkit-0.1.0-py3-none-any.whl` и sdist успешно собраны.
- В wheel проверены `py.typed`, девять `_tools` файлов, METADATA и лицензии.
- Wheel установлен отдельно; все восемь entry points прошли `--help` smoke.
- Release workflow отклоняет тег, не совпадающий с `pyproject.toml`.
- Официальный PyPI JSON endpoint 2026-08-05 вернул 404 для `forex-toolkit`, но
  окончательную доступность подтверждает только PyPI при создании проекта.

Настройка и безопасный порядок выпуска описаны в `RELEASE-CHECKLIST.md`.

## Внешние действия владельца

1. Установить giscus App и раскомментировать готовую конфигурацию по `GISCUS-SETUP.md`.
2. Настроить Trusted Publisher на PyPI.
3. Отдельно подтвердить подписанный тег `v0.1.0`; автоматизация сама тег не создаёт.

## Следующий приоритет после недели данных

### J. Улучшение маршрута по метрикам · P0 · S-M

Сравнить события `calculator_completed`, `risk_exposure_completed`,
`trade_plan_saved`, `journal_import_completed`, `replay_completed`,
`exam_completed` и feedback. Исправлять первый крупный провал воронки, а не
добавлять контент вслепую.

Возможные решения по сигналу:

- слабый вход в инструменты: новый блок действий на главной и в dashboard;
- `first15_completed` низкий: упростить первый проблемный шаг;
- планы сохраняют, но не открывают: улучшить прямую кнопку и подсказку;
- сделки открывают, но не закрывают: упростить форму разбора;
- много `feedback_no`: разобрать страницы по pathname в GoatCounter.

## Отложенный бэклог

- тренажёр свечных паттернов;
- поглавные и ежедневные квизы, достижения;
- XLSX-импорт после реальных обезличенных фикстур;
- live-корреляции и календарь только с надёжным источником и offline fallback;
- серверная синхронизация прогресса только после явного запроса пользователей.

## Рабочий порядок

`метрики 7+ дней -> найти провал -> одно изменение -> e2e -> повторно измерить`

Следующие крупные функции до этого сигнала не добавляются. Исключения — защита
данных, исправления доступности и подтверждённые ошибки расчётов.
